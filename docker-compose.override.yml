# Development overrides — applied automatically by `docker compose up`
services:
  app:
    build:
      context: .
      target: runtime
    volumes:
      - ./src:/app/src:ro
      - ./model:/model:ro
      - ./alembic:/app/alembic:ro
      - ./.env:/app/.env:ro
    environment:
      LOG_LEVEL: DEBUG
